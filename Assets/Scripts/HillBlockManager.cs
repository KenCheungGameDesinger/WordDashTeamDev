using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HillBlockManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // float speed = Random.Range(.3f, 1.3f);
        // HillObject[] hillObjects = GetComponentsInChildren<HillObject>();
        // foreach(HillObject hillObject in hillObjects)
        // {
        //     hillObject.speed = speed;
        // }
    }
}
